# Supabase — Frontend (publishable key only, never service role)
# After `pnpm db:start`, copy values from CLI output into .env.local
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
